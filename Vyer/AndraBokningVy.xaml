
<UserControl x:Class="Labb1_OOP.Vyer.AndraBokningVy"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*" />
            <ColumnDefinition Width="1*" />
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        
        <StackPanel Grid.Row="0" Grid.Column="0">

        <TextBlock Grid.ColumnSpan="2"
                Text="SpelCenter" />
            
            <ComboBox ItemsSource="{Binding TidVal}"
                SelectedItem="{Binding ValdTid, Mode=TwoWay}"/>

            <TextBlock Text="StartDatum"/>
            
            <DatePicker Text="Datum"
                SelectedDate="{Binding ValdDatum, Mode=TwoWay}" /> 

            <ListBox ItemsSource="{Binding Tider}">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Button
                            Content="{Binding}"
                            Command="{Binding DataContext.TestaAndraBokningCommand,
                                            RelativeSource={RelativeSource AncestorType=ListBox}}"
                            CommandParameter="{Binding}" />
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>

        </StackPanel>

        <StackPanel Grid.Row="0" Grid.Column="1">

            <Button Content="Återvänd till meny" 
                Command="{Binding GaTillMenyCommand}"/>

            <TextBlock 
                Text="{Binding NuvarandeBokning}"/>
            
            <TextBlock Text="plats" />
            <TextBox Text="{Binding Plats}" />

            <TextBlock Text="maxantal" />
            <TextBox Text="{Binding MaxAntal}" />

            <TextBlock Text="beskrivning" />
            <TextBox Text="{Binding Beskrivning}" />

            <Button Content="Ändra"
                Command="{Binding TestaAndraBokningCommand}"
                CommandParameter="{x:Null}"/>
            

        </StackPanel>
    </Grid>
</UserControl>